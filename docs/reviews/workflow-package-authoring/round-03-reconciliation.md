# Round 03 reconciliation

Reviewed candidate: `bda0d2ca00b60d5228b2b0b023389f5668c3b44f`, tree `02ee6335d1f21e27982922212f6d9657ba68c221`.

The independent report was frozen before reconciliation, SHA-256 `09de54fd045c19527e71c39a21a6f89d4680867e7b28cf1503ccee8b0fddea4d`. Verdict: **BLOCK**, three Important findings and one Minor. Preserve the original report unchanged.

| ID | Disposition | Severity | Controller assessment and correction | Status |
| --- | --- | --- | --- | --- |
| R03-001 | Accepted | Important | Derive command resource kinds from the authenticated contract and dispatch both ordinary and loop consumers through command analysis, including extensionless and non-Markdown filenames. | Corrected in `daae2e4`; five regression cases and combined affected gate pass. |
| R03-002 | Accepted | Important | UTF-8 decoding alone admits NUL/control-containing binary content to the text editor; editing normalizes unrelated bytes. Route such content through binary metadata/replacement while preserving ordinary text support and exact replacement bytes. | Corrected in `8fd78f6`; App, bridge/capture, and native artifact regressions pass. |
| R03-003 | Accepted | Important | Add Workflow submits fixed source destinations without controls to change them. Preserve captured source authority while allowing explicit distinct destination paths and workflow names, with collision/revision guards and an exact preview. | Corrected in `3d75eaa`; all 43 destination tests pass, including the bounded-review anchor correction. |
| R03-004 | Accepted; correction selected | Minor | App omits reference props, so the promised command-consumer view necessarily reports a false empty result. Wiring this existing surface is a bounded feature correction, not additional scope; verify refresh and package isolation. | Corrected in `8fd78f6`; actual App saved-consumer, refresh, and package-isolation checks pass. |

The reviewer independently passed 166 TypeScript tests across 20 files and 22 native tests, including all 13 package Git tests. The initial zero-match Rust filter is not coverage. Green selections do not refute the independent production-path counterexamples.

## Remediation evidence

- Command dispatch: initial regression run had four failures and one passing shared-consumer control. After correction, all 24 semantic readiness tests passed. The first combined run had 62 passes and one expected failure in the concurrently added binary-capture regression; it was not a passing gate. The subsequent seven-file editor/bridge/capture/semantic/reference/readiness gate passed all 91 tests in 59.96 seconds.
- Add Workflow: initial tests reproduced the second-blank/common-example collision and missing destination controls (three failures, 33 passes). Additional tests reproduced alias-name inspection, stale existing-name expectations, and read-only existing-definition handling (three failures, 36 passes). The initial corrected creation/dialog/controller selection passed all 39 tests in 30.28 seconds; the strengthened final selection passed all 43 tests in 28.72 seconds. Correction commit: `3d75eaa`.
- App integration: the original regression run reproduced missing References and missing binary Replace surfaces. The corrected three-case selection passed in 32.23 seconds: invalid UTF-8 and valid-UTF-8 binary replacement preserve exact replacement hashes; extensionless loop commands show saved consumers, disclose unsaved changes without saving them, refresh after saved edits, and isolate packages. Intermediate reruns exposed a test-literal encoding error and a detached-element assertion; those test defects were corrected without relaxing timeouts.
- Native binary regression: the new real-file test initially accepted NUL-containing bytes as text. After rejecting C0 controls except TAB/LF/CR and DEL following strict UTF-8 decoding, all 14 `workspace::tests::artifact` tests passed (59.46 seconds compile, 0.71 seconds tests). Unicode and BOM text remain supported; native and browser policies match. This Windows selection does not establish manual OS-open or cross-platform acceptance.
- Bounded independent destination review found a further name-copy issue: an anchored `name` can be referenced by a prompt, and changing the anchor would change that prompt too. The regression run reproduced acceptance of the unsafe rename (two failures, 20 passes). Copy renaming now refuses anchored names before patching, with explicit guidance; unchanged-name copies and Moves preserve their bytes. The reviewer inspected the correction and found the issue addressed, with no new actionable findings. The separate bounded artifact/reference source review found no actionable issues. Neither review counts as an additional full-feature round.
- Global `check` passed with zero errors/warnings; `lint`, `format:check`, build (10.33 seconds), bundle budget (1,308,457 bytes minified / 341,141 gzip), and `git diff --check` passed. Both package documentation tests passed. These affected checks do not replace the final complete gate.

References deliberately describe authenticated saved workflow files. They do not implicitly save drafts or claim to represent unsaved edits. Loading and unavailable reference analysis are explicit. Copy allows a distinct workflow name through the existing YAML patcher; Move preserves the source name and bytes. Destination overrides remain separate from captured source identity, and existing names are bound to revision expectations.

All three Important findings and the selected Minor correction are addressed. Six Chromium package-authoring journeys passed in one minute with one worker and zero retries, including binary replacement, complete bundled resource copy, keyboard navigation, and reduced motion. Round 04 may proceed on the committed immutable candidate. Full final verification remains required after all five rounds; installed-app manual acceptance and platform limitations remain open. This reconciliation is not a release claim.
