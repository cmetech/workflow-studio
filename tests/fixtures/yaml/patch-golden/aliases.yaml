defaults: &defaults
  timeout: 30
  nested:
    enabled: true
    label: keep
metadata:
  inherited: *defaults
  unchanged: *defaults
nodes:
  - id: prepare
    command: prepare
  - id: finish
    depends_on: [prepare]
    prompt: finish
