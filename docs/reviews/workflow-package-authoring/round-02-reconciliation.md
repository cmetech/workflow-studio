# Round 02 reconciliation

Reviewed candidate: `5f5350a40a3d6ff4cea689ecf449350776d6495a`, tree `c273ea4fbdd0673e738ad73907c049f4284e8d55`.

The independent report was frozen before reconciliation, SHA-256 `7e5f2de07e3576488cb8fb9c25d19aca85e3212b93f1e953374e797bd62ccba1`. Verdict: **BLOCK**, two Important findings and one Minor. The original report remains unchanged.

| ID | Disposition | Severity | Controller verification and correction | Status |
| --- | --- | --- | --- | --- |
| R02-01 | Accepted | Important | Ordinary artifact write reaches identity-only quarantine disposal in `write_stream_impl`, separate from corrected transaction disposal. Preserve the live original inode before any destructive cleanup, and carry success/error recovery locations through the actual artifact editor. Final hash checks alone cannot close the race. | Corrected in `5f7a97a` (native) and `73f9558` (frontend). Windows and Linux affected workspace gates pass; independent bounded follow-up found no remaining actionable defect. |
| R02-02 | Accepted | Important | `artifacts::open` performs blocking open before checking regular-file metadata. Reject Unix special files without blocking, preserve no-follow and opened-handle validation, and cover replacement after enumeration. | Corrected in `5f7a97a`; actual Linux five-mode regression and final 112-test workspace gate pass. |
| R02-03 | Accepted; correction selected | Minor | Add Artifact passes busy without disabling ModalShell dismissal; Escape/backdrop can hide pending failure/recovery output. This bounded correction directly supports the required recovery-receipt flow and does not add feature scope. | Escape, backdrop and native cancel regressions failed before the correction and pass afterward, including eventual failure/receipt display and focus restoration. |

The native test gate passed 103 tests, the selected frontend gate passed 204 tests across 18 files, and the final package Git filter passed 13 tests in 587.80 seconds. Those existing tests do not disprove the production-path counterexamples. The reviewer explicitly distinguished its Linux OS-primitive probes from actual Rust/Tauri integration; the latter is required for the FIFO correction.

Round 02 remediation is complete. Both Important findings and the selected Minor correction are verified, permitting round 03 on the next clean immutable candidate. The original report remains BLOCK for its reviewed candidate. The final full gate remains required after all five review rounds.

## Frontend remediation evidence

The busy-dialog cases first failed all three dismissal assertions. The controller receipt cases first failed because successful and failed ordinary saves had no retained receipt state. After correction, the affected controller, dialog, bridge and bounded receipt helper gate passed **45 tests across four files**, exit 0, in 61.18 seconds. Successful receipts survive later receiptless saves and artifact navigation until explicit dismissal; failure receipts preserve the original error and dirty draft. Dismissal only clears the display.

Two actual-App tests first failed because neither save outcome displayed the recovery location. The App now displays the shared recovery details in its application notices. Both tests passed, exit 0, in 41.12 seconds, checking exact locations and explicit dismissal; the failed-save case also checks the original error. An earlier attempt stopped in the unchanged canvas import hook before exercising the behavior and is not counted as a regression failure. Native builds were paused for the subsequent quiet App window. No hook or interaction threshold changed.

Frontend corrections are committed in `73f9558`. A separate bounded read-only review of that diff and its callers found no actionable defects; it ran no tests and excluded native Rust. It confirmed that receipts clear on workspace disposal and do not persist as application state across restart. This does not count as round 03. Global Svelte/TypeScript checks reported zero errors and warnings, global lint passed, and the production build passed in 16.05 seconds. The unchanged startup bundle budget passed at 1,306,796 bytes / 340,590 gzip.

## Native reproduction evidence

The expanded Windows artifact-save regressions observed three failures: rollback discarded the installed live inode, an early failure discarded the externally written staged inode, and retained original permissions changed. The other four cases passed. Corrections passed all seven cases in 0.62 seconds. A subsequent postcommit-error permission regression also failed before the common exit restoration was added; it passes in the final affected gate.

An isolated Linux source tree was created from `5f5350a` with only a tests patch (SHA-256 `3a80255dc70e8099618c08c2b83e3efc77214d32eaa5e060431b571f509db8c2`). Rust/Cargo 1.88.0 compiled the locked dependency graph successfully. The actual `artifact_fifo_read_and_bound_replacement_are_bounded` native test then failed in 5.03 seconds because the artifact read did not reject a FIFO before its deadline: zero passed, one failed, 322 filtered. This is native regression evidence, distinct from the reviewer's earlier OS-primitive probe. The initial corrected snapshot passed the same test in 0.55 seconds, including all five subprocess modes (read, bound read, identity, permission restoration and replacement during save). Its expanded artifact filter passed seven tests in 0.94 seconds. The final corrected snapshot also passes the affected workspace gate recorded below.

The bounded independent native source review identified another R02-01 disposal path: after Studio links the staged inode to its target, an external replacement of the temporary pathname makes the move helper roll back by removing the target, which is now that inode's last name. Later retention through the replaced temporary pathname cannot recover it. This differs from an external process deleting the sole name before Studio links it. The deterministic actual-save regression failed at the missing retained destination. A guarded rollback callback now retains that destination before unlinking it; if retention fails, it preserves and reports the verified workspace target. The expanded case covers creation, overwrite, unavailable recovery storage, later open-handle writes and foreign-stage preservation. All nine focused Windows tests passed in 0.84 seconds.

The bounded independent follow-up found this issue addressed and no new actionable defect in the correction. It inspected source only, with Git blob identities `3177b0d758bbd62da8f8e6fa935f372f6b7dab4b` (`files.rs`), `416206013ef83050f93178b449c83e3c2abe9ed1` (`artifacts.rs`), and `304992511e3dab90feb313471f453c5e981fb071` (`artifact_write_tests.rs`). It confirmed successful commits bypass retention of the current staged inode. This follow-up is not a full review round. Final affected Windows/Linux workspace gates pass on the same reviewed source blobs.

## Final affected native verification

Native correction commit: `5f7a97a`. Windows `cargo test --offline --manifest-path src-tauri/Cargo.toml workspace:: -- --test-threads=1` passed **112 tests**, zero failures or ignored tests, in 159.42 seconds. The final isolated Linux snapshot, using pinned Rust/Cargo 1.88.0 and the unchanged lockfile, passed **112 workspace tests**, zero failures or ignored tests, in 17.78 seconds after 2m25s compilation. The filter excludes unrelated Git and other native tests; these are affected gates, not full-suite claims. Linux includes the five FIFO subprocess modes and staged-name replacement regression. Windows includes its permission-specific regressions.

The final Linux source SHA-256 values were `4d927a9ce2ce638ffdf874012b95b5be96a2355f04efc8411e311788f0098f7e` (`files.rs`), `ca6b39648cae00b7c3d469db311ebdfcb2f937d38a8655ede141417a1e94e44f` (`artifacts.rs`), and `7af4811625c2bdfc33da2a9fa8091dd496a6ffc0dba3af8f369d0a52f59d22d0` (`artifact_write_tests.rs`). The parent verified the matching Git blob identities before commit.

The Linux run uses Ubuntu 22.04 on WSL2; it does not exercise Windows-only code or establish macOS acceptance. Same-inode recovery remains subject to the documented vault availability and volume limits. Recovery files remain live, with no automatic purge. Installed native WebView/manual OS acceptance and the final full corrected-candidate gate remain separate requirements.
