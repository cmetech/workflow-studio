# Round 05 reconciliation

Reviewed candidate: `a43abc5f067a519820b71a04b6218fdbd7a01dd7`, tree `e9a28df3d533c90437f37d09c67f970f0e7fba68`.

The independent report was frozen before reconciliation, SHA-256 `157cf1c7917a91245d8f6d7ec553242dda8d88523bfe8bd57cbf047da8e23f27`. Verdict: **BLOCK**, two Important findings. Preserve the report unchanged.

| ID | Disposition | Severity | Assessment | Status |
| --- | --- | --- | --- | --- |
| R05-01 | Accepted | Important | A schema-valid manifest with a missing declared workflow or companion loses its package projection and repair action. Preserve explicit access to the existing manifest while membership is invalid; restore navigation after correcting it. Preparation must remain blocked. | Corrected in `52df418`; targeted independent follow-up found no actionable issue. |
| R05-02 | Accepted | Important | Binding recovery to app-data storage makes existing artifact saves fail for normal writable workspaces on another volume. Select safe same-filesystem retention without sacrificing live-inode protection, persistent provenance, containment, budgets, or fail-closed handling. | Corrected in `e1c6f7b`; targeted independent immutable follow-up passed. |

The report passed 656 unit tests across 68 files, 62 native tests, 14 Chromium checks, and static/contracts/examples/resources/build/bundle gates. The missing-member repair defect was independently reproduced in production modules and Chromium. Cross-volume failure follows the production storage/save path and an injected cross-device error; physical two-volume UI acceptance was not performed. Green tests do not invalidate these counterexamples.

Both findings agree with the existing product behavior requirements. Documentation of a same-volume limitation did not authorize a general inability to save artifacts in another local drive. Any correction must retain the safety properties established by prior remediation, including recovery of later writes through already-open handles; copying and deleting the original is insufficient.

After corrections: commit exact paths, obtain targeted independent review of the immutable corrected candidate, and run the final complete gate. Required installed-app/manual acceptance remains separate and unverified. No merge, push, publication, or release is implied.

## Remediation in progress

- R05-01 browser regression reproduced the missing repair button before production changes: the missing-member alert appeared, preparation was absent, and clicking the expected `Repair manifest` button timed out under the unchanged 30-second CI budget (30.2 seconds, exit 1). The regression then requires editing the original manifest in Advanced Source, saving it, reopening the surviving workflow, and validating the restored package without changing other files. Unit regressions and correction are in progress.
- R05-02 correction will preserve storage outside the selected workspace and package output. The bounded design prefers app-data storage when suitable, otherwise selects a deterministic private same-filesystem vault under a usable workspace ancestor. It must validate ownership, permissions, directory identities and links, preserve live-inode recovery and provenance, retain existing budgets, and never purge automatically. Filesystem-root workspaces or nested mounts with no safe same-filesystem location outside the workspace remain explicit fail-closed capability boundaries; no reserved package-output exclusion is introduced. Actual cross-filesystem Linux and unavailable-app-data regressions are being prepared before implementation.

R05-01 focused correction results: three files reproduced eight failures with 21 existing tests passing (17.43 seconds). This included missing definition/companion repair and unsafe finding-path admission. Discovery now exposes only path-validated existing rejected-package manifests for repair; tree and controller consume that explicit list. All 29 tests then passed (21.16 seconds), with exact-file formatting and diff checks clean. The parent browser regression passed in 14.1 seconds (6.1-second test body): Advanced Source repair, exact original manifest saved, restored workflow navigation, successful validation, and all saved files unchanged from before the introduced typo. Static checks, independent corrected-candidate review, and commit remain pending.

R05-02 regression evidence: the Windows unavailable-app-data save test failed at `workspace_recovery_unavailable`. In an isolated unchanged baseline plus regression tests, Linux failed both unavailable-app-data and actual cross-filesystem cases, including `EXDEV` (errno 18) between `/tmp` and `/dev/shm`. Both runners exited with the expected failures before the correction. These are native operation tests, not installed-app UI acceptance.

R05-01 final affected checks: global type checking passed with zero errors/warnings, lint and formatting passed, all four selected App metadata/recovery/preparation/example flows passed (25.52 seconds, 89 excluded), and both documentation checks passed (8.17 seconds). The correction and guide/browser regression were committed as `52df41816dedb0e476a44c842eb02de26eca995a`, tree `42a148f9bffd68ac3d6be7ff2a6a9086aa1ecbb1`. The fresh-context, source-only independent assessment in `round-05-repair-followup.md` found no actionable issue in that immutable correction and unchanged consumers; it did not independently rerun the tests or review concurrent native changes.

The preliminary native follow-up identified a remaining Important admission gap before commit: opening an existing readable recovery directory does not prove that new retained files can be created there. A read-only primary can therefore be selected instead of a usable outside-workspace fallback. The existing unavailable-primary regression used an obstructing regular file, not denied directory write permission. A real permission-denial regression and correction are required before closing R05-02; earlier Windows/Linux passing groups do not cover this counterexample. The preliminary review was source-only against recorded working-tree hashes and is not yet the required immutable corrected-candidate follow-up.

The new actual Windows DACL regression reproduced denied entry creation and then failed the save with OS error 5 because the unwritable primary was selected. An initial `ReOpenFile` admission approach rejected writable directories too (nine failures among 13 selected recovery cases); this intermediate run is not a pass. The replacement uses a non-mutating capability-relative directory open with the required write/traverse rights and verifies the opened identity. That selection passed all 13 Windows recovery tests, including the original permission counterexample and full-store no-bypass coverage. Unix admission uses effective-access checking relative to the bound directory. Final affected groups and immutable independent review remain pending. The rejected approach created empty test vault directories outside fixture roots; exact locations are being recorded for verified nonrecursive cleanup, separately from all retained user data and previously blocked cleanup targets.

All 20 empty test-created vaults from that intermediate failure were removed nonrecursively. Before deletion, the parent checked each resolved absolute path against the exact five-parent/four-name allowlist, verified the recorded creation timestamp, rejected reparse points, and verified zero entries. `Directory.Delete(path, false)` ensures a directory that becomes nonempty cannot be recursively removed. All 20 paths were then confirmed absent; the ignored ledger retains the creation and removal receipts. No retained recovery files or earlier policy-blocked cleanup targets were touched.

## Final correction verification

The candidate-verification failure regression first reproduced continued creation at a second ancestor, confined within its owned temporary fixture. The correction stops ancestor search after a newly created vault fails verification, preserving that evidence without further directory creation.

Final affected native commands used `cargo test --offline --manifest-path src-tauri/Cargo.toml workspace:: -- --test-threads=1` (Linux also retained `--nocapture` output). **Windows: 120 passed, 87.21 seconds. Linux: 124 passed, 12.92 seconds**, final isolated snapshot `/tmp/loop24-package-browser-nJb4gESJ/r05-green-9c7OZnmI`. Linux explicitly reported effective UID 1000 and actual primary directory-creation denial. The suite includes the real `/tmp`/`/dev/shm` save, late writes through retained handles, private storage, restart provenance, no budget bypass, failure containment, and prior rollback/FIFO regressions. Cargo.lock stayed unchanged at SHA-256 `e088a516d9dc48486d575e75d163c5ec0f8ea77e3c34b46b117642b89e32321b`.

Exact Rust formatting and diff checks passed. Both updated-guide documentation tests passed in 9.90 seconds. Native code and the guide were committed as `e1c6f7bc55f06416ec6e58330389148b36be8186`, tree `3b1331fa09eff80307846044b5e1b0a0316bbd2f`. The independent follow-up verified the reviewed hashes and Git-cleaned blobs against that immutable commit, inspected the guide, and returned **PASS** with no remaining findings. Its source-only scope and platform limits are recorded in `round-05-recovery-followup.md`.

Both round-05 Important findings are corrected and independently assessed. All five full review rounds are complete. The final complete gate, fresh pinned-agent interoperability run, and required installed-app/manual acceptance remain open; the original round-05 report remains BLOCK for its original candidate.
