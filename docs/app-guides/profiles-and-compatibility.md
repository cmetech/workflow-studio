# Profiles and compatibility

Choose a language profile in the companion YAML and read compatibility findings before using deferred fields.

```yaml
language_compatibility: archon-2026-07
```

Profile-disallowed fields are structural blockers. Runtime support and migration readiness remain compatibility information from the contract, not execution promises. See [Companion policy](#contract:companion-policy).
