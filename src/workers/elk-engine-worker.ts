/// <reference lib="webworker" />

// ELK's supported worker entry installs its official algorithm message handler.
// Vite emits this local asset with the application; no external URL is used.
import 'elkjs/lib/elk-worker.min.js'
