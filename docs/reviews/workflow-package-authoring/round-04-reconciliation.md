# Round 04 reconciliation

Reviewed candidate: `cf1fe25bee98369ed138b8f5dd2206cd830ed4d1`, tree `2220a282762e90394f0a94e2c4612d62f756b18f`.

The independent report was frozen before reconciliation, SHA-256 `257af27b887a03c1e4deb451c1718f1eef91048d90c96bbe654e5d9cc7ee0cf7`. Verdict: **BLOCK**, one Important finding and two Minor. Preserve the report unchanged.

| ID | Disposition | Severity | Assessment | Status |
| --- | --- | --- | --- | --- |
| R04-01 | Accepted | Important | Git can implicitly fetch missing promisor objects during local reads. Enforce the local-only boundary for every read/mutation path and verify failure without transport, helper, or authentication activity using synthetic repositories. | Corrected in `2250040`; focused, Git-module, and actual integration tests passed. |
| R04-02 | Accepted; correction selected | Minor | Native preview trims a message that the frontend later compares against untrimmed input. Normalize once before writes and bind preview, display, and authorization to the same message. | Corrected in `8357a92`; focused regression checks passed. |
| R04-03 | Accepted; correction selected | Minor | The approved package row and overview Git comparison is absent. Add scoped local change counts/path comparison and proposed version with honest loading/unavailable states and refresh/isolation guards. | Corrected in `8357a92`; focused, App, and browser checks passed. |

The reviewer passed 399 TypeScript tests across 55 files, 34 native package tests, 14 Chromium checks, and eight static/build gates. The initial zero-match native filter is not coverage. The additional local promisor probe demonstrated the boundary failure despite those passes. Browser checks use the adapter and do not establish installed-app behavior or non-Windows native acceptance.

## Remediation evidence in progress

- Local-only Git: four substantive promisor regressions failed before correction (plus one passing fixture entry; 37.90 seconds tests, 27.11 seconds compile). Both command constructors now force lazy-fetch suppression and an empty protocol allowlist. The focused selection passed all five entries in 56.57 seconds after 43.63 seconds compile. An independent bounded source review found no actionable issue across package reads, previews, isolated-index mutations, and ref updates. Broader Git/integration checks remain in progress. Older Git may internally start a fetch before refusing all transport; a capability-aware regression must distinguish that safe fallback from actual helper/transport activity. No older binary execution is claimed yet.
- Message authorization: three-file regression run reproduced five failures with 26 existing tests passing (26.39 seconds), including native-compatible trim mismatch, controller input, and dialog display. The shared boundary-whitespace rule now runs before preparation and is used for displayed input, preview comparisons, and commit authorization; interior multiline content is retained. The corrected selection passed all 31 tests in 24.55 seconds. Explicit expected-string test fixtures and global checks remain to be rerun. A bounded independent source review found no actionable issues; it did not execute tests or count as a full round.
- Git summary: the first command used an old ambient Node and failed at test startup; that is not regression evidence. With the configured Node path, the new summary suite failed because its module was absent, and the overview behavior test failed at the missing change count while four existing tests passed. Implementation is in progress.
- Actual App summary regression also failed at the missing package-row count after setup completed (one failure, 92 excluded). The bounded summary review identified an additional first-version proposal that bypassed ready analysis; that correction is awaiting its failing/passing test. Contract replacement now invalidates proposed versions alongside existing saved-file/workspace invalidation. Full captures are retained only while a summary is loading, then discarded after applying or rejecting the proposal.

Subsequent verification on September 23:

- All 85 native Git module tests passed in 1,214.09 seconds. Zero-match main/integration binaries are excluded from that coverage; the actual integration selection remains pending. Two negative-case fixtures printed `fatal: Needed a single revision` and then passed their expected assertions; no test failed. Production Git guards were unchanged during the run.
- The first-version regression reproduced a proposed `1.2.3` despite a missing declared workflow. Real package capture rejected with `package_member_missing`; proposals now remain absent until matching ready analysis, including unborn repositories. The independent reviewer confirmed the correction, contract invalidation, and capture disposal by source inspection, with no new actionable findings.
- The seven-file frontend run passed 48 cases and failed one new test's incorrect major-version expectation. The fixture already declared the workflow; adding its missing files correctly suggests minor `1.3.0`, not major `2.0.0`. After correcting only that expectation, all eight summary tests passed; the other six files had passed all 41 tests. This covers 49 focused cases, including message normalization, shared proposal extraction, components, and documentation.
- Both selected App flows passed in 27.38 seconds (91 excluded): saved modification, explicit validation proposal, refreshed committed baseline, and actual local-commit completion followed by automatic zero-change refresh. No timeouts were relaxed.

Final remediation checks on September 23:

- The capability-aware promisor selection passed all five entries again in 51.14 seconds after 27.51 seconds compilation. Production guards remained unchanged after the full 85-test run. Actual native dispatch and integration binaries passed 1 and 12 tests respectively (0.03 and 213.36 seconds). Exact Rust formatting and diff checks passed. Git 2.55.0.windows.3 was tested; older behavior was simulated by removing lazy-fetch suppression, with no older binary execution claimed.
- Global static checking initially found one test-fixture literal widened to `string`. Adding the explicit `PackageGitSummary` map type corrected the fixture; checking then passed with zero errors and warnings. Lint and formatting passed. Build passed in 11.74 seconds; the bundle budget passed at 1,400,924 minified and 374,972 gzip bytes. No production behavior changed for the type correction.

- All 14 Chromium package authoring, preparation, recovery, and performance cases passed in 1.7 minutes with CI timeouts, one worker, and zero retries. The 250-node/500-edge case had 104 files, 864.7 ms cold and 179.3 ms concurrent readiness, zero long tasks, and zero authority work over 30 pointer frames. These browser-adapter results do not establish installed-app/manual acceptance.

The corrected tree fixture passed all three tests after its type annotation (9.90 seconds). All selected corrections are committed in `2250040` and `8357a92`. Round 04 remediation is complete; round 05 and full final verification remain required. The original report remains unchanged and BLOCK for its reviewed candidate.
