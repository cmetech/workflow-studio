# Package marketplace interoperability receipt — 2026-09-22

The bounded Task 16 Step 5 acceptance passed on WSL2 Ubuntu 22.04 with CPython 3.11.15. The pinned agent accepted unchanged Studio-produced package bytes through public distribution/index validation, compilation, review, installation, explicit trust, and update. Updating the payload and version invalidated trust. This receipt does not complete Task 16, the full release gate, platform testing, or the five required adversarial review rounds.

## Immutable inputs and backend

- Agent production source: `3e89c2659b6e9c95a627b8f819ff63a11529d86a`, materialized using `git archive --format=tar` from the read-only `hermes-agent-resource-contract` checkout. Its checkout HEAD was `5c8c4cd4b2a727730b85d6e76cbc603d2125fd01`; only two review documents differ from the archive pin. No sibling source was edited. Its eight pre-existing untracked evidence files remained present.
- Studio generation checkout HEAD when captured: `53a21d232fd669acbc81bbe37bfb9921b47bcc5d`. The producer byte hashes below identify the actual working-tree code, including any uncommitted changes at generation time.
- WSL2 Ubuntu 22.04, kernel `6.18.33.2-microsoft-standard-WSL2`, Git `2.34.1`, CPython `3.11.15` (Clang 22.1.3).
- Agent `_HAS_DESCRIPTOR_WALK` was explicitly asserted true. It requires `O_NOFOLLOW`, `open` and `stat` with `dir_fd`, and `scandir` with an FD.
- Isolated Linux root: `/tmp/loop24-marketplace-interop-w3v67psa`; successful destination: its `configured/hermes-home` directory. Source and bare Git repositories were under `configured/publisher` and `configured/source.git`.
- Windows receipts: `C:/Users/ecorell/AppData/Local/Temp/loop24-marketplace-interop-d9749515524546fbbf5a687fe422a79b` (`studio-generation.json`, `agent-probe.json`, `linux-acceptance.json`, `acceptance.py`, and both generated version directories). Temporary paths are operational receipts, not permanent repository artifacts; the relevant input hashes, results, and executable acceptance harness are preserved here.

The runtime was provisioned without global installation using the official [uv release binaries](https://docs.astral.sh/uv/getting-started/installation/), version `0.11.32`. Download: `https://github.com/astral-sh/uv/releases/download/0.11.32/uv-x86_64-unknown-linux-gnu.tar.gz`; observed archive SHA-256: `aab924fd522efd06f1c5f3b93a243864fc453132c94b2dc49f1371b528a4b967`. Cache, managed Python, and virtual environment were isolated. Direct dependencies were Pydantic `2.13.4`, PyYAML `6.0.3`, python-dotenv `1.2.2`, Rich `14.3.3`, HTTPX `0.28.1`, and psutil `7.2.2`. These were sufficient for the real production import and admission path; no stub assessment builder was substituted.

## Studio production generation

The input was the complete eight-file `examples/packages/laptop-diagnostic` package. A Node process using `node.exe --import tsx --input-type=module` loaded real bundled contracts through `loadPackageExampleContracts`, read exact UTF-8 example files through `readPackageExampleFiles`, and called these production APIs:

1. `replaceManifestProperty(manifestText, 'version', version)` for versions `1.0.0` and `1.0.1`.
2. For `1.0.1` only, append `\n# Interoperability v2: synthetic payload revision.\n` to `scripts/analyze-snapshot.py`. The script was never executed.
3. `capturePackageExample(example, contracts)` for the complete package, including resource resolution and static analysis.
4. `prepareGeneratedPackageFiles({ contract, manifestText, snapshot, analysis, analyzedHashes, committedIndexText, workingIndexText, workingIndexHash })`. Version 1 used null index context; version 2 used version 1's generated index and its SHA-256. All returned writes were applied under the corresponding temporary version directory.

Both analyses reported `ready: true`, no blockers, and `execution_unverified` / `runtime_unverified` advisories. The generated bytes were copied to the Linux filesystem without modification. No hand-written replacement digest or index was supplied to the agent.

| Version | Distribution digest | Marketplace index SHA-256 |
| --- | --- | --- |
| 1.0.0 | `1693f24bc6fc1a731469ad1711f148ca4f481a5e7099ce89e8ee9acfed96ff5d` | `fbefddbf2244b3b985c4e6c5b2c050caa277b1b45481380521987b9e9eba0599` |
| 1.0.1 | `8d36c532b181ceed938af5f5e71a74499fa84228d3b119cfc4d8e92aa216607e` | `9994d33d7376697cac7779b556aa1655e84bd08364e1023c1a5aeef029c91cff` |

| Producer | SHA-256 |
| --- | --- |
| `src/lib/packages/digest.ts` | `4ecf81dfcfa6e3961c69a91fe8889ee874dbc9915b8b7773f06b3d5094453f12` |
| `src/lib/packages/marketplace-index.ts` | `470b2bf1b63b26c6a62b77df870f7c2948946b9b64a4985062731dc598f20dc8` |
| `src/lib/packages/preparation.ts` | `421d77e023694bc71c7c881a82ebcb7e1ad122340364f41883547f6bd09f9fb0` |
| `src/lib/examples/package-example-analysis.ts` | `c8b2e41464f584c4439b73fe6da8498aff0b674e140edd0220d76cfd943a91c3` |
| `src/features/packages/package-analysis.ts` | `d7b3afaae0e2538a9487e1f32ca2e9ca0809dfc22a502cfdd8c3398e81ff0a75` |

## Actual admission, install, and trust results

The initial empty destination configuration passed public package/index validation and compilation, then `prepare_install` rejected with `package_review_blocked`. Real `service.inspect` identified `model_reference_missing`: the AI command node had neither an explicit model nor a destination default model. Pinned `provider_authority.py::_resolved_route` first falls back to `default_runtime.model`; `runner_binding.py::_production_provider_snapshot` reads the destination configuration. This is a destination admission boundary, not a package digest failure or a new Studio static-readiness requirement.

The successful run used exactly the same generated package bytes and an isolated `HERMES_HOME/config.yaml` containing only:

```yaml
model:
  provider: openrouter
  default: openai/gpt-4.1-mini
```

This is a concrete route identifier for static admission, not a claim that the model was available. No API key, credential, or provider request was used. The service still used its normal production assessment builder and empty declared runtime/tool/service/secret availability.

| Check | Observed result |
| --- | --- |
| `load_distribution` and `load_repository_index`, both versions | Passed on the descriptor-safe Linux backend |
| `parse_workflow_source_bytes`, `WorkflowCatalogSnapshot.capture`, `compile_workflow`, both versions | Passed without executing any node |
| Registered local `file://` source refresh | `fresh` |
| `prepare_install` / `confirm_install` | Version `1.0.0`; no structural blockers |
| Trust after installation | `{'laptop-diagnostic': 'untrusted'}` |
| `review_trust` / `grant_trust` | `{'laptop-diagnostic': 'trusted'}` |
| `prepare_update` | `update_available`, candidate `1.0.1`; modified script and manifest reported |
| `confirm_update` | Version `1.0.1` |
| Trust after update | `{'laptop-diagnostic': 'untrusted'}` |

Temporary source commits were `7d721d7a1ffe4ee5f3264686b6ed1fd4739626f5` (v1) and `dc3d41136ac1027839dc74c2cbe8892fab38fd01` (v2). The local bare source was updated by a local `git fetch <temporary-publisher> main:main`; no push or network Git transport was used. These operations exercise the external agent's existing marketplace implementation, not new Studio remote-Git functionality.

The harness installed a Python audit hook rejecting every subprocess except Git and rejecting socket connect/bind events. It set `GIT_ALLOW_PROTOCOL=file`, `GIT_CONFIG_GLOBAL=/dev/null`, and `GIT_CONFIG_NOSYSTEM=1`. Its successful receipt contains only Git subprocesses. No workflow runner, authored Python script, command Markdown, provider request, or MCP service was invoked. Agent temporary checkout directories under `/tmp/hermes-workflow-marketplace-*` were managed by its normal temporary-directory contexts.

## Native Windows boundary and setup limitation

A separate guarded Windows Python 3.11.9 probe passed strict manifest/digest/index parsing, independently recomputed both distribution digests using the agent implementation, verified claims, parsed the workflow, and compiled it. Public `load_distribution` correctly failed with `package_digest_mismatch: descriptor-safe package traversal is unavailable`; `O_NOFOLLOW`, `open/stat` directory-FD support, and `scandir` FD support were absent. Native Windows review/install/update was not claimed. Windows Studio authoring support does not establish Windows agent backend traversal support.

An initial bootstrap command expanded its working-directory variable on the Windows side and created task-only `cache`, `python`, and `venv` directories under `C:/Users/ecorell/Developer/work/cmetech/github.com/cmetech/workflow-studio`. Their creation times and contents were verified as belonging to this bootstrap. Automatic tool policy rejected cleanup with `rejected: blocked by policy`; no further reason was supplied. They were retained and reported to the parent, not silently deleted through another mechanism. The successful acceptance used explicit Linux absolute paths. No tracked source or existing user configuration was changed by bootstrap.

## Executed acceptance command and harness

After provisioning and copying the generated inputs, the successful command was:

```powershell
wsl.exe -d Ubuntu-22.04 -- /tmp/loop24-marketplace-interop-w3v67psa/venv/bin/python -I -B /mnt/c/Users/ecorell/AppData/Local/Temp/loop24-marketplace-interop-d9749515524546fbbf5a687fe422a79b/acceptance.py
```

It exited `0`. The following is the executed harness; a fresh run must use fresh temporary paths and regenerate its inputs through the Studio APIs above. It intentionally leaves isolated receipts for inspection.

```python
from pathlib import Path
import os,sys,json,subprocess,shutil
root=Path('/tmp/loop24-marketplace-interop-w3v67psa')
base=root
root=base/'configured'
root.mkdir()
for version in ['1.0.0','1.0.1']: shutil.copytree(base/version,root/version)
(root/'hermes-home').mkdir()
(root/'hermes-home/config.yaml').write_text('model:\n  provider: openrouter\n  default: openai/gpt-4.1-mini\n')
os.chdir(root)
os.environ.update(HERMES_HOME=str(root/'hermes-home'),XDG_CONFIG_HOME=str(root/'config'),GIT_CONFIG_GLOBAL='/dev/null',GIT_CONFIG_NOSYSTEM='1',GIT_ALLOW_PROTOCOL='file',PYTHONDONTWRITEBYTECODE='1')
sys.path.insert(0,str(base/'agent'))
from plugins.workflow.marketplace.package import load_distribution,load_repository_index,_HAS_DESCRIPTOR_WALK
from plugins.workflow.marketplace.models import InstallRequest,WorkflowMarketplaceSource
from plugins.workflow.marketplace.service import WorkflowMarketplaceService
from plugins.workflow.schema import parse_workflow_source_bytes
from plugins.workflow.compilation import WorkflowCatalogSnapshot,compile_workflow
calls=[]
def audit(event,args):
    if event=='subprocess.Popen':
        executable,argv,cwd,env=args
        if Path(str(executable)).name!='git': raise RuntimeError('Forbidden execution: '+str(args))
        calls.append({'argv':argv,'cwd':str(cwd)})
    if event in ('socket.connect','socket.bind'): raise RuntimeError('Network forbidden')
sys.addaudithook(audit)
def git(where,*args):
    return subprocess.check_output(['git',*args],cwd=where,stderr=subprocess.STDOUT,text=True).strip()
receipt={'python':sys.version,'descriptor_safe_walk':_HAS_DESCRIPTOR_WALK,'agent_pin':'3e89c2659b6e9c95a627b8f819ff63a11529d86a','versions':{}}
assert _HAS_DESCRIPTOR_WALK
for version in ['1.0.0','1.0.1']:
    package=root/version/'packages/laptop-diagnostic'
    distribution=load_distribution(package)
    index=load_repository_index(root/version)
    source=parse_workflow_source_bytes(package/'workflows/laptop-diagnostic.yaml',workflow_bytes=(package/'workflows/laptop-diagnostic.yaml').read_bytes(),sidecar_bytes=(package/'workflows/laptop-diagnostic.hermes.yaml').read_bytes(),package_root=package,sidecar_path=package/'workflows/laptop-diagnostic.hermes.yaml')
    compiled=compile_workflow(source,WorkflowCatalogSnapshot.capture([source]))
    receipt['versions'][version]={'distribution':'passed','index':'passed','compile':'passed'}
work=root/'publisher'
shutil.copytree(root/'1.0.0',work)
git(work,'init','--initial-branch=main')
git(work,'config','user.name','Isolated Interop')
git(work,'config','user.email','interop@example.invalid')
git(work,'add','.')
git(work,'commit','-m','Synthetic v1')
receipt['v1_commit']=git(work,'rev-parse','HEAD')
remote=root/'source.git'
git(root,'clone','--bare',str(work),str(remote))
service=WorkflowMarketplaceService(root/'hermes-home',profile='interop')
service.add_source(WorkflowMarketplaceSource(name='isolated',repositoryUrl=remote.as_uri()))
assert service.refresh_source('isolated').state=='fresh'
review=service.prepare_install(InstallRequest(identifier='isolated/laptop-diagnostic'),actor='interop')
assert not review.assessment.blockers
installed=service.confirm_install(review.confirmation_token,actor='interop')
assert installed.version=='1.0.0'
receipt['installed_trust']=service.workflow_trust(installed.identity)
assert set(receipt['installed_trust'].values())=={'untrusted'}
trust=service.review_trust(installed.identity,actor='interop')
service.grant_trust(trust.confirmation_token,actor='interop')
receipt['granted_trust']=service.workflow_trust(installed.identity)
assert set(receipt['granted_trust'].values())=={'trusted'}
shutil.copytree(root/'1.0.1',work,dirs_exist_ok=True)
git(work,'add','.')
git(work,'commit','-m','Synthetic v2')
receipt['v2_commit']=git(work,'rev-parse','HEAD')
# Local bare source only; no push or network operation.
git(remote,'fetch',str(work),'main:main')
update=service.prepare_update(installed.identity,actor='interop')
assert update.result=='update_available'
assert update.candidate_version=='1.0.1'
receipt['changes']=[{'path':item.path,'kind':item.kind} for item in update.file_changes]
updated=service.confirm_update(update.confirmation_token,actor='interop')
assert updated.version=='1.0.1'
receipt['updated_trust']=service.workflow_trust(updated.identity)
assert set(receipt['updated_trust'].values())=={'untrusted'}
receipt['subprocesses']=calls
receipt['authored_execution']=False
(root/'acceptance.json').write_text(json.dumps(receipt,indent=2)+'\n')
print(json.dumps(receipt,indent=2))

```
