# Companion policies

Store optional delivery and policy metadata in the companion file, not in the definition graph.

```yaml
language_compatibility: archon-2026-07
required_services:
  - issue-tracker
```

The editor checks companion structure. Service availability, secrets, and admission are operational advisories. See [Companion policy](#contract:companion-policy).
